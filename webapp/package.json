{
  "name": "ict-trader-webapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Investment-grade SPA for the ICT Trading Bot — Svelte + Vite, deployed to GitHub Pages, browser-direct to the bot's HTTPS API.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.4",
    "playwright": "^1.61.1",
    "svelte": "^5.19.0",
    "svelte-check": "^4.1.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.1"
  }
}
